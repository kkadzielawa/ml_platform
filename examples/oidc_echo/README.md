# OIDC echo example

This example is a deliberately small HTTP application used by `02.03` to study OpenID Connect protection.

It demonstrates login redirects, token exchange, userinfo-based bearer-token validation, and a realm-role check for an admin-only path.

It is a study fixture, not a production web framework or hardened session implementation.
