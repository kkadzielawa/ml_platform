# Execution prompt for issue 00.03

Copy the prompt below into the lesser model's session from the repository root.

---

You are implementing backlog issue `00.03 — Create the repository skeleton` in the current repository.

Your task is deliberately narrow: create a minimal installable Python project skeleton with one smoke test. Do not add any ML, data, service, container, Kubernetes, CI, or infrastructure implementation.

## Required context to inspect

Read these files before editing:

1. `issues/README.md`
2. `issues/phase-00/00.03-repository-skeleton.md`
3. `docs/charter.md`, which is the required output of dependency `00.01`

If `docs/charter.md` does not exist, stop without editing and report that dependency `00.01` is incomplete.

Inspect the current contents of every allowed path before changing anything. Preserve unrelated existing work. Do not assume the repository is clean or empty.

## Allowed paths

You may create or modify only:

- `adr/**`
- `docs/**`
- `contracts/**`
- `config/**`
- `src/**`
- `tests/**`
- `examples/**`
- `Makefile`
- `pyproject.toml`
- `.gitignore`

Do not modify `IMPLEMENTATION_PLAN.md`, anything under `issues/**`, or any other path. Do not initialize Git, commit, download application code, or install system-wide software.

## Exact target tree

Create the following minimal tree, merging carefully with anything that already exists:

```text
adr/
  README.md
docs/
  README.md
  charter.md                 # preserve; do not rewrite
contracts/
  README.md
config/
  README.md
src/
  ml_platform/
    __init__.py
tests/
  test_smoke.py
examples/
  README.md
Makefile
pyproject.toml
.gitignore
```

Do not create empty future directories such as `infra`, `clusters`, `platform`, `components`, `pipelines`, `runtimes`, or `services`; later issues own them.

## Implementation requirements

1. Configure a standard `src`-layout Python package in `pyproject.toml`:
   - distribution name: `ml-platform-study`;
   - import package: `ml_platform`;
   - initial version: `0.1.0`;
   - minimum Python: `>=3.11`;
   - build backend: setuptools;
   - no runtime dependencies;
   - a `test` optional dependency containing pytest with a bounded range, not `latest` and not a wildcard. Issue `00.04` will create the authoritative version catalog and tighten pins.
2. Keep `src/ml_platform/__init__.py` free of application logic. Export `__version__ = "0.1.0"` only.
3. Add one focused pytest smoke test that imports `ml_platform` and asserts its version is exactly `0.1.0`.
4. Add a `.PHONY` `test` target to `Makefile`. It must run tests through the active Python interpreter using `python -m pytest` and must not start external services.
5. Each placeholder README must contain only:
   - the directory's purpose;
   - what kind of artifacts will eventually belong there;
   - a note that implementation belongs to later backlog issues.
6. Add a conservative Python `.gitignore` covering bytecode, virtual environments, test/coverage caches, build artifacts, and local environment files. Do not ignore source, tests, lockfiles, manifests, general YAML/JSON, datasets, or model artifacts with broad wildcard rules.
7. Do not add license text, CI configuration, pre-commit configuration, linters, formatters, Docker files, lockfiles, application code, or extra tests in this issue.

## Verification

Run the following commands from the repository root:

```bash
python -m pip install -e '.[test]'
make test
python -c "import ml_platform; assert ml_platform.__version__ == '0.1.0'"
```

If dependency installation fails solely because network access is unavailable, do not change dependency declarations to bypass the failure. Report the blocked command and still run `make test` only if pytest is already available.

## Acceptance criteria

- All required paths exist and no unrequested future directories were created.
- The package installs in editable mode from a clean Python environment.
- `make test` passes without PostgreSQL, object storage, containers, network services, credentials, or GPUs.
- The smoke test imports the installed `src`-layout package rather than accidentally importing a repository-root module.
- No file outside the allowed paths changed.
- Existing `docs/charter.md` content is unchanged.

## Required final response

Return a concise report with:

1. files created or modified;
2. commands run and their exact pass/fail result;
3. confirmation that `docs/charter.md` was preserved;
4. assumptions made;
5. any blocker or remaining risk.

Do not claim completion if installation or `make test` fails.

