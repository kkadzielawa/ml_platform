---
id: "08.10"
phase: "08"
title: "Train a small adapter"
status: "hardware"
depends_on: ["08.09", "07.08", "04.02"]
route: "required"
---

# 08.10 — Train a small adapter

## Outcome

Implement Transformers/PEFT/TRL supervised fine-tuning through the selected training runtime or a single-device fallback.

## Allowed paths

- `components/train_peft/**`
- `pipelines/llm_finetune/**`
- `tests/components/train_peft/**`

## Deliverables

- Implement Transformers/PEFT/TRL supervised fine-tuning through the selected training runtime or a single-device fallback.
- Log data/model/config revisions, losses, checkpoints, and adapter artifact.

## Acceptance

- Tiny overfit fixture demonstrates the loop works.
- Normal fixture completes within declared budget and emits no merged full weights.

## Verify

```bash
make test-peft-component && make run-peft-baseline
```

## Non-goals

- Preference optimization or full fine-tuning.

