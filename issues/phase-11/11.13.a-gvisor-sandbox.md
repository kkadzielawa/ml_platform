---
id: "11.13.a"
phase: "11"
title: "Add gVisor-isolated code execution"
status: "hardware"
depends_on: ["11.01", "11.11"]
route: "choose-one:11.13"
---

# 11.13.a — Add gVisor-isolated code execution

## Outcome

Only if approved use case requires code: run pinned image in one-shot Kubernetes Job with gVisor, no credentials, read-only root, limits, network deny, and output cap.

## Allowed paths

- `clusters/dev/sandboxes/gvisor/**`
- `services/agent_runtime/tools/code/**`
- `tests/security/agent_sandbox/**`
- `docs/agents/sandbox.md`

## Deliverables

- Only if approved use case requires code: run pinned image in one-shot Kubernetes Job with gVisor, no credentials, read-only root, limits, network deny, and output cap.
- Destroy sandbox after result collection.

## Acceptance

- Escape/network/resource-bomb fixtures are blocked within study test limits.
- Approval is required for every code job.

## Verify

```bash
make test-agent-sandbox
```

## Non-goals

- Claiming perfect isolation or shell access to platform namespace.

