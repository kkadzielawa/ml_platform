---
id: "10.13"
phase: "10"
title: "Implement a resumable re-embedding migration"
status: "hardware"
depends_on: ["10.08", "10.10"]
route: "required"
---

# 10.13 — Implement a resumable re-embedding migration

## Outcome

Build a new candidate index from immutable chunks using a second pinned embedding revision, with checkpoints and cost estimate.

## Allowed paths

- `pipelines/rag_reembed/**`
- `components/reembed/**`
- `tests/pipelines/rag_reembed/**`
- `docs/rag/reembedding.md`

## Deliverables

- Build a new candidate index from immutable chunks using a second pinned embedding revision, with checkpoints and cost estimate.
- Evaluate before alias switch.

## Acceptance

- Old active index remains queryable throughout.
- Resume avoids recomputing completed batches.

## Verify

```bash
make test-rag-reembedding
```

## Non-goals

- Selecting a better model without evaluation.

