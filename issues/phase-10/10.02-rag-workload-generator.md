---
id: "10.02"
phase: "10"
title: "Build a representative RAG scale fixture"
status: "ready"
depends_on: ["10.01", "09.01"]
route: "required"
---

# 10.02 — Build a representative RAG scale fixture

## Outcome

Generate deterministic documents/chunks/vectors/ACLs/queries matching the recorded distributions at small and target sizes.

## Allowed paths

- `tests/performance/rag/generator/**`
- `tests/performance/rag/workloads/**`
- `docs/rag/scale-fixture.md`

## Deliverables

- Generate deterministic documents/chunks/vectors/ACLs/queries matching the recorded distributions at small and target sizes.
- Include known relevant results for correctness sampling.

## Acceptance

- Same seed produces identical manifest/checksums.
- Generator stays within configured disk/memory ceilings.

## Verify

```bash
make generate-rag-scale-fixture && make test-rag-scale-fixture
```

## Non-goals

- Benchmarking a database.

