# IMP-06.02 — Define a reachable CPU route to RAG and bounded agents

Status: proposed; not implemented by this review.
Priority: P2 — planning enhancement.
Dependencies: None; reviewer approves route before implementation.

## Evidence and impact

The blueprint names CPU-first profiles and llama.cpp, but 08.04 only has hardware-marked vLLM/SGLang alternatives. Later evaluation/benchmark tasks depend on adaptation/quantization work. 09.08 is hardware-marked despite requesting a small local embedding model. The local component runner 04.13 depends on the orchestrated pipeline 04.11.

Read [the review README](README.md) first. Recheck these observations against the working tree before implementation.

## Proposed allowed paths

`issues/INDEX.md`; selected `issues/phase-04/**`, `issues/phase-08/**`, `issues/phase-09/**`, `issues/phase-11/**`; `adr/**`; `docs/implementation/**`.

## Bounded implementation steps

Write a proposed dependency diff first. Add one explicit CPU serving alternative only after model/runtime/license selection is reviewed, with measured limits and the same API contract. Separate base-model inference/evaluation from adapter-training prerequisites. Split local-runner execution from later remote-parity verification. Preserve GPU adaptation and distributed training as distinct learning branches. Keep embeddings, RAG ACLs and agent budgets on the CPU route.

## Acceptance and verification

Run the backlog validator and trace a complete dependency path from existing local data work to CPU retrieval, cited answers and a bounded tool run. No node on that path should silently require GPU/cloud spending. Record deferred parity/hardware checks instead of falsely marking them passed.

## Non-goals and stop conditions

Do not implement the runtime, download weights, or remove security/evaluation gates in this planning task.

If a required API, fixture, version or permission is unavailable, report the exact blocker. Do not fabricate a successful result. Hand off changed files, verification mode/outcomes and remaining risks; commit/push only when requested.

