# IMP-02.04.b — Record the custom Polars validator as the selected route

Status: proposed; not implemented by this review.
Priority: P2 — alternative requiring a route decision.
Dependencies: Reviewer selects this instead of IMP-02.04.a.

## Evidence and impact

The current implementation is already a small custom Polars validation engine. Retaining it can suit the laptop, but claiming the GX library performed checks is inaccurate.

Read [the review README](README.md) first. Recheck these observations against the working tree before implementation.

## Proposed allowed paths

`adr/**`; `src/ml_platform/data_quality/**`; `config/great_expectations/**`; `tests/data_quality/**`; `docs/data/quality.md`; `pyproject.toml`; `issues/phase-03/03.07.a-great-expectations.md` only with the explicit route-change approval.

## Bounded implementation steps

Record the choice and its learning tradeoff. Rename report executor identity and explain the supported custom suite format. Remove the unused GX dependency only after checking all consumers. Keep compatibility aliases for public import paths where necessary. Correct the original route documentation without pretending the original GX integration was implemented.

## Acceptance and verification

All current positive/negative quality tests must pass with no GX import. Preserve aggregation and redaction. Record unsupported expectation behavior as a clear error. Run the backlog validator if issue metadata changes.

## Non-goals and stop conditions

Do not select this route merely to make a failing GX integration test pass. The reviewer owns the change in learning goal.

If a required API, fixture, version or permission is unavailable, report the exact blocker. Do not fabricate a successful result. Hand off changed files, verification mode/outcomes and remaining risks; commit/push only when requested.

