# IMP-02.04.a — Execute the selected Great Expectations engine

Status: proposed; not implemented by this review.
Priority: P1 — preferred route.
Dependencies: Choose exactly one IMP-02.04 route.

## Evidence and impact

`great_expectations_validation.py` imports gx for its version but dispatches all expectations to custom Polars functions. Docs and result metadata identify the engine as Great Expectations.

Read [the review README](README.md) first. Recheck these observations against the working tree before implementation.

## Proposed allowed paths

`src/ml_platform/data_quality/**`; `config/great_expectations/**`; `tests/data_quality/**`; `docs/data/quality.md`; `pyproject.toml` only if the chosen engine needs an explicit optional extra.

## Bounded implementation steps

Use the currently pinned Great Expectations API to execute one real batch and its supported built-in expectations. Keep the compact aggregate report adapter. Isolate the custom leakage-name check and label its executor accurately. Map the existing suite semantics explicitly, including the multi-column custom configuration shape; do not assume it is already a native GX suite.

## Acceptance and verification

Tests must demonstrate a real GX validation result is consumed, plus good data and each existing seeded defect. Assert no raw failing rows appear in output. Run `.venv/bin/python -m pytest tests/data_quality tests/integration/ingestion`. If this exceeds one session, first adapt one supported expectation, leaving the task explicitly incomplete.

## Non-goals and stop conditions

Do not deploy a server or use GX Cloud. Do not implement route b simultaneously.

If a required API, fixture, version or permission is unavailable, report the exact blocker. Do not fabricate a successful result. Hand off changed files, verification mode/outcomes and remaining risks; commit/push only when requested.

