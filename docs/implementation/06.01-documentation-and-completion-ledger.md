# IMP-06.01 — Make implemented scope and completion evidence navigable

Status: proposed; not implemented by this review.
Priority: P1 — documentation clarity; can start immediately.
Dependencies: None.

## Evidence and impact

`docs/README.md` remains a skeleton placeholder. The backlog states ready means executable, not done. OpenMetadata's runbook says quality and lineage are cataloged and lists three scripts under a heading saying two. Several issue allow lists omit Makefile even though verification expects new make targets.

Read [the review README](README.md) first. Recheck these observations against the working tree before implementation.

## Proposed allowed paths

`docs/README.md`; `docs/runbooks/openmetadata.md`; new `docs/implementation/completion-ledger.md`; `issues/README.md`; `issues/ISSUE_TEMPLATE.md`; only specifically audited issue files.

## Bounded implementation steps

Create a documentation entry map and a completion ledger with selected route, source revision/dirty state, test mode, latest evidence timestamp and remaining gaps. Correct OpenMetadata claims using IMP-01.04/01.05 status. For upcoming issues, explicitly allow exact shared integration files such as Makefile and version declarations when needed. Do not broadly waive all path restrictions. Link this review as an addendum rather than changing historical completion evidence.

## Acceptance and verification

Check all relative links. Run `python3 issues/validate_backlog.py` for backlog edits. Have each completion claim point to code and a reproducible command, distinguishing fixture/offline, rendered and live checks. Historical reports must retain their original timestamps.

## Non-goals and stop conditions

Do not mark unverified issues complete, renumber existing issue IDs, or present rendered stage/prod examples as deployed environments.

If a required API, fixture, version or permission is unavailable, report the exact blocker. Do not fabricate a successful result. Hand off changed files, verification mode/outcomes and remaining risks; commit/push only when requested.

