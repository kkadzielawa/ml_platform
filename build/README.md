# Build assets

This directory contains local build definitions and generated build notes for study fixtures.

`02.10` owns the BuildKit-based fixture image build. Later issues own SBOM generation, vulnerability scanning, signing, and admission policy.
