rules:
  - id: ml-platform-forbidden-test-secret
    category: secret
    title: ML Platform forbidden seeded test secret
    severity: HIGH
    regex: 'ML_PLATFORM_FORBIDDEN_SECRET_[A-Z0-9]{16,}'
    keywords:
      - ML_PLATFORM_FORBIDDEN_SECRET
