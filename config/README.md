# Config

Purpose: hold configuration files for local and platform workflows.

Artifacts: example settings, version catalogs, environment templates, and policy configuration.

Implementation belongs to later backlog issues.
