# CloudNativePG chart values

This directory holds local Helm configuration for the CloudNativePG operator in the Phase 1 study cluster.

The operator chart is pulled from the official CloudNativePG Helm repository at the pinned chart version recorded in the `Makefile`.

HA, backups, point-in-time recovery, poolers, and application-specific databases belong to later backlog issues.
