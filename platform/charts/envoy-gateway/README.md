# Envoy Gateway chart values

This directory holds local Helm configuration for the selected Phase 1 Gateway API implementation.

The chart itself is pulled from the upstream Envoy Gateway OCI chart repository at the pinned version recorded in the `Makefile`.

Implementation beyond the local study install belongs to later backlog issues.
