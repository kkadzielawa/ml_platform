# cert-manager chart values

This directory holds local Helm configuration for cert-manager in the Phase 1 study cluster.

The chart is pulled from the official cert-manager OCI chart repository at the pinned version recorded in the `Makefile`.

Production PKI, ACME, and external trust distribution belong to later backlog issues.
