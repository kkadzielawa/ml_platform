# External Secrets chart values

This directory records the pinned local External Secrets Operator Helm values for the `02.05.a` study secrets route.

The operator syncs one scoped credential from OpenBao into the example project namespace.

Production-grade secret stores, multi-tenant controller policies, and broader secret migration belong to later backlog issues.
